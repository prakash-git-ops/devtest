name: "Hello World Workflow"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello gyana"
        run: echo "Hello,world!"