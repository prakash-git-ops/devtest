name: "Hello hii World Workflow"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello prakash"
        run: echo "Hello,world prakash jena!"